<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HighThemeTest" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>

        <item name="android:colorBackground">@color/background_color</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textColorPrimary">@color/primary</item>
        <item name="android:statusBarColor">@color/navi_background</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- 정의한 attr-->
        <item name="text_primary">@color/text_primary</item>
        <item name="text_secondary">@color/text_secondary</item>
        <item name="text_tertiary">@color/text_secondary</item>
        <item name="icon_img">@drawable/icon</item>
    </style>

    <style name="Theme.HighThemeTest" parent="Base.Theme.HighThemeTest" />

</resources>
